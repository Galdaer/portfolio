fastapi==0.111.0
uvicorn[standard]==0.35.0
pydantic==2.11.7
httpx==0.27.2
aiohttp==3.11.7

# Only minimal dependencies required for thin forwarding layer.
# (All business logic and persistence lives in healthcare-api.)
