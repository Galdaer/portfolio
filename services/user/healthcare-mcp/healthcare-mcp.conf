# Healthcare MCP Server - DEPRECATED (2025-08-13)
# This service is now integrated into healthcare-api container for reliable stdio communication
# The separate container approach failed due to MCP Python client expecting subprocess spawning
# Healthcare MCP server is now available at /app/mcp-server/build/index.js inside healthcare-api container

# DEPRECATED - DO NOT START THIS SERVICE
# Use healthcare-api service instead which includes integrated MCP server

# Original configuration preserved for reference:
# image=intelluxe/healthcare-mcp:latest
# description=Healthcare MCP server in pure stdio mode for Open WebUI pipeline integration
# env=MCP_TRANSPORT=stdio-only,FHIR_BASE_URL=http://172.20.0.13:5432,PUBMED_API_KEY=test,CLINICALTRIALS_API_KEY=test
# volumes=/home/intelluxe/logs:/app/logs,healthcare-mcp-stdio-data:/app/data
# network_mode=intelluxe-net
# static_ip=172.20.0.18
# restart_policy=unless-stopped
# healthcheck=ps aux | grep -v grep | grep -q "node.*index.js"
# command=node /app/build/index.js
# working_dir=/app
# read_only=false
# security_opt=no-new-privileges
# user=1000:1001
